<Page
    x:Class="FufuLauncher.Views.MapPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:helpers="using:FufuLauncher.Helpers"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid x:Name="AppTitleBar" 
              Grid.Row="0" 
              Height="44" 
              Padding="12,0,0,0"
              Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
              BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
              BorderThickness="0,0,0,1">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="138"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <TextBlock Text="{helpers:Loc Key=MapPage_Title}" 
                           Style="{StaticResource CaptionTextBlockStyle}"
                           VerticalAlignment="Center" 
                           FontWeight="SemiBold"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <Button x:Name="RefreshMapToggle" 
                              Content="{helpers:Loc Key=MapPage_Refresh}" 
                              FontSize="12"
                              Height="32"
                              Click="RefreshMapToggle_Click"/>
                
                <ToggleButton x:Name="LockMapToggle" 
                              Content="{helpers:Loc Key=MapPage_LockMapOff}" 
                              FontSize="12"
                              Height="32"
                              Click="LockMapToggle_Click"/>

                <ToggleButton x:Name="TopMostToggle" 
                              Content="{helpers:Loc Key=MapPage_TopMostOff}" 
                              FontSize="12"
                              Height="32"
                              Click="TopMostToggle_Click"/>

            </StackPanel>
        </Grid>

        <WebView2 x:Name="MapWebView" Grid.Row="1"/>
    </Grid>
</Page>