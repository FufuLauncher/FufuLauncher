<Page
    x:Class="FufuLauncher.Views.PluginConfigPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:FufuLauncher.Views"
    xmlns:helpers="using:FufuLauncher.Helpers"
    mc:Ignorable="d"
    Background="Transparent">

    <Page.Resources>
        <ThemeShadow x:Name="SharedShadow" />

        <AcrylicBrush x:Key="CardAcrylicBrush"
                      TintColor="{ThemeResource SolidBackgroundFillColorBase}"
                      TintOpacity="0.6"
                      FallbackColor="{ThemeResource SolidBackgroundFillColorBase}" />
    </Page.Resources>

    <Grid Padding="36,32,36,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="8" Margin="0,0,0,24">
            <StackPanel Orientation="Horizontal" Spacing="16">
                <Button Click="OnBackClick" Style="{StaticResource NavigationBackButtonNormalStyle}" />
                <StackPanel Spacing="2">
                    <TextBlock x:Name="TitleTextBlock" Text="{helpers:Loc Key=PluginConfigPage_Title}" Style="{StaticResource TitleTextBlockStyle}" />
                    <TextBlock Text="{helpers:Loc Key=PluginConfigPage_AdjustParams}" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.6" />
                </StackPanel>
            </StackPanel>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,16,32">
            <StackPanel Spacing="24">

                <Grid x:Name="InfoBanner"
                      Background="{ThemeResource LayerFillColorAltBrush}"
                      CornerRadius="8" Padding="16">
                    <StackPanel Spacing="12">
                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <FontIcon Glyph="&#xE946;" FontSize="16"
                                      Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                            <TextBlock Text="{helpers:Loc Key=PluginConfigPage_PluginInfo}" Style="{StaticResource BodyStrongTextBlockStyle}" />
                        </StackPanel>
                        <ItemsControl x:Name="InfoItemsControl">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,2">
                                        <TextBlock Text="{Binding}" Style="{StaticResource BodyTextBlockStyle}"
                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                   FontSize="13" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Grid>

                <GridView x:Name="ConfigGridView" SelectionMode="None" IsItemClickEnabled="False">
                    <GridView.ItemContainerStyle>
                        <Style TargetType="GridViewItem">
                            <Setter Property="Margin" Value="0,0,16,16" />
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="CornerRadius" Value="12" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="GridViewItem">
                                        <ContentPresenter />
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </GridView.ItemContainerStyle>

                    <GridView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <ItemsWrapGrid Orientation="Horizontal" ItemWidth="340" MaximumRowsOrColumns="-1" />
                        </ItemsPanelTemplate>
                    </GridView.ItemsPanel>

                    <GridView.ItemTemplate>
                        <DataTemplate x:DataType="local:ConfigOption">
                            <Grid CornerRadius="12"
                                  Background="{StaticResource CardAcrylicBrush}"
                                  BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                  BorderThickness="1"
                                  Shadow="{StaticResource SharedShadow}"
                                  Translation="0,0,8">

                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="RootGrid.Translation" Value="0,-2,16" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>

                                <Grid x:Name="RootGrid" Padding="20" MinHeight="130">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <TextBlock Text="{x:Bind NameDisplay}"
                                               Style="{StaticResource BodyStrongTextBlockStyle}"
                                               TextTrimming="CharacterEllipsis"
                                               ToolTipService.ToolTip="{x:Bind NameDisplay}"
                                               FontSize="15" />

                                    <ContentControl Grid.Row="2"
                                                    Margin="0,16,0,0"
                                                    Content="{x:Bind EditControl}"
                                                    HorizontalContentAlignment="Stretch"
                                                    VerticalAlignment="Bottom" />
                                </Grid>
                            </Grid>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>